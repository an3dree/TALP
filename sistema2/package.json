{
  "name": "aqys-alunos",
  "version": "1.0.0",
  "description": "Sistema de gerenciamento de alunos e avaliações",
  "private": true,
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "postbuild": "npm run copy:frontend",
    "copy:frontend": "mkdir -p backend/dist/src/public && cp -r frontend/dist/* backend/dist/src/public/",
    "start": "node backend/dist/src/server.js",
    "start:prod": "npm run build && npm start",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix backend"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
