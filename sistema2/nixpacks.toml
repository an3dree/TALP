# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix backend", "npm install --prefix frontend"]

[phases.build]
cmds = ["npm run build --prefix frontend", "npm run build --prefix backend"]

[start]
cmd = "NODE_ENV=production node backend/dist/server.js"
